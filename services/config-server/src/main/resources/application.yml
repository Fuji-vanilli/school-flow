server:
  port: 8000

spring:
  application:
    name: config-server

  cloud:
    compatibility-verifier:
      enabled: true

    config:
      server:
        git:
          uri: https://github.com/Fuji-vanilli/school-flow-repo.git